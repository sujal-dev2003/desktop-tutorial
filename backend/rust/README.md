# Rust desktop/API starter

Rust is a good fit for a Tauri desktop shell or a small Axum service. A future implementation can expose the same read-only atlas routes and a favorites command, with SQLite access kept behind a repository module.

This folder is documentation-only for now. It avoids adding a Rust toolchain requirement to the static site while leaving a clear path for a Tauri desktop build.
